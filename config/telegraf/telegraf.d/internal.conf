# Telegraf's own metrics. internal_agent.metrics_dropped is the only visible
# signal that the output stage discarded a metric after the AMQP message was
# already acknowledged, which is precisely what Phase 2's experiment D hunts.
# This is observability, not tuning: it changes nothing about buffering or
# acknowledgement behaviour.
[[inputs.internal]]
  collect_memstats = false
