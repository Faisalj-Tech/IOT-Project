[pytest]
testpaths = tests
addopts = -v --strict-markers -m "not experiment"
markers =
    stack: test requires the docker compose stack to be running
    experiment: slow reliability experiment; deselected by default, run with -m experiment
