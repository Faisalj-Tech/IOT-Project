# Dev-only credentials. Never reuse outside this project.
RABBITMQ_ADMIN_USER=admin
RABBITMQ_ADMIN_PASSWORD=adminpass
RABBITMQ_DEVICE_USER=device
RABBITMQ_DEVICE_PASSWORD=devicepass
RABBITMQ_TELEGRAF_USER=telegraf
RABBITMQ_TELEGRAF_PASSWORD=telegrafpass

INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=influxadminpass
INFLUXDB_ORG=iot
INFLUXDB_BUCKET=telemetry
INFLUXDB_TOKEN=dev-token-0123456789abcdef

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=grafanapass

# Shared across all cluster nodes; Erlang refuses to cluster without a match.
# Dev-only value, same status as the credentials above.
RABBITMQ_ERLANG_COOKIE=dev-cluster-cookie-change-me
# ignore | pause_minority — Phase 3 measures both (spec 5.H, 5.I).
RABBITMQ_PARTITION_HANDLING=ignore

# Phase 4 region credentials. Dev-only, same status as the values above.
# The password_hash values in definitions.region.json are derived from these;
# changing a password here means regenerating its hash with
# scripts/rmq_password_hash.py and updating that file too.
RABBITMQ_DEVICE_EU_PASSWORD=devicepass-eu
RABBITMQ_DEVICE_US_PASSWORD=devicepass-us
RABBITMQ_TELEGRAF_EU_PASSWORD=telegrafpass-eu
RABBITMQ_TELEGRAF_US_PASSWORD=telegrafpass-us

# Phase 5 security. Dev-only, same status as every credential above.
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=keycloakadminpass
KEYCLOAK_REALM=iot
# Client secrets are also declared in config/keycloak/realm-iot.json; changing one
# means changing both.
KEYCLOAK_TELEGRAF_EU_SECRET=telegraf-eu-secret
KEYCLOAK_TELEGRAF_EU_SHORT_SECRET=telegraf-eu-short-secret
