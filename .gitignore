.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.env
config/certs/
data/
*.log

# Phase 5 dev PKI. Generated by scripts/make_certs.py; never committed.
# Private keys must not enter git history, and committed certs would expire silently.
certs/

# Phase 6: per-replica swarm reports and Locust output, bind-mounted by
# compose.load.yml. The aggregated result JSONs are committed; these are not.
docs/results/_swarm/
docs/results/_locust/
